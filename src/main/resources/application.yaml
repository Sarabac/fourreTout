spring: 
  datasource:
    url: jdbc:mariadb://localhost:3306/morpion
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver


mybatis:
   config-location:
     - classpath:mybatis/mybatis-config.xml
   mapper-locations:
     - classpath:mybatis/mapper/*.xml
   type-aliases-package: com.yllies.morpion.model
