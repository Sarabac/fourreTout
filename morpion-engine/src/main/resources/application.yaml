spring: 
  datasource:
    url: jdbc:mariadb://localhost:3306/morpion
    username: user
    password: pass
    driver-class-name: org.mariadb.jdbc.Driver


mybatis:
   config-location:
     - classpath:mybatis/mybatis-config.xml
   mapper-locations:
     - classpath:mybatis/mapper/*.xml
   type-aliases-package: com.ultramorpion.model

server:
  port: 8080